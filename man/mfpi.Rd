% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mfpi.R
\name{mfpi}
\alias{mfpi}
\alias{mfpi.default}
\alias{mfpi.formula}
\title{Model Interactions Between a Categorical and Continuous Covariates}
\usage{
mfpi(x, ...)

\method{mfpi}{default}(
  x,
  y,
  group_var = NULL,
  cont_vars = NULL,
  flex = c("flex1", "flex2", "flex3", "flex4"),
  p_interact = 0.05,
  min_improvement = NULL,
  include_group_var = FALSE,
  show_models = FALSE,
  weights = NULL,
  offset = NULL,
  cycles = 10,
  scale = NULL,
  shift = NULL,
  df = 4,
  center = TRUE,
  subset = NULL,
  family = c("gaussian", "poisson", "binomial", "cox"),
  criterion = c("pvalue", "aic", "bic"),
  select = 0.05,
  alpha = 0.05,
  force_keep = NULL,
  xorder = c("ascending", "descending", "original"),
  fp_powers = NULL,
  ties = c("breslow", "efron", "exact"),
  strata = NULL,
  nocenter = NULL,
  acd_vars = NULL,
  zero_vars = NULL,
  use_ftest = FALSE,
  control = NULL,
  verbose = TRUE,
  digits = 3,
  ...
)

\method{mfpi}{formula}(
  formula,
  data,
  catvar = NULL,
  adjcatvar = FALSE,
  flex = c("flex1", "flex2", "flex3", "flex4"),
  linearvar = NULL,
  fp1var = NULL,
  fp2var = NULL,
  showmodel = FALSE,
  weights = NULL,
  offset = NULL,
  cycles = 5,
  scale = NULL,
  shift = NULL,
  df = 4,
  center = TRUE,
  subset = NULL,
  family = c("gaussian", "poisson", "binomial", "cox"),
  criterion = c("pvalue", "aic", "bic"),
  select = 0.05,
  alpha = 0.05,
  keep = NULL,
  xorder = c("ascending", "descending", "original"),
  powers = NULL,
  ties = c("breslow", "efron", "exact"),
  data_input = c("original", "equidistant"),
  strata = NULL,
  nocenter = NULL,
  ftest = FALSE,
  control = NULL,
  verbose = TRUE,
  digits = 3,
  ...
)
}
\arguments{
\item{x}{A numeric matrix of dimension \eqn{n \times p}, where rows are
observations and columns are variables. Column names are required. The
matrix must not contain an intercept column; binary variables should be
coded as 0/1; multi-level categorical variables (other than \code{group_var})
should be expanded into dummy variables beforehand. \code{x} must be free of
missing values.}

\item{...}{Currently unused. Reserved for future extensions.}

\item{y}{The response vector (or matrix for survival data). For Gaussian, binomial,
and Poisson families, supply a numeric or factor vector of length \eqn{n}.
For Cox models, supply a two-column \code{\link[survival:Surv]{survival::Surv()}} object. Must have
the same number of observations as \code{x}.}

\item{group_var}{A single character string naming the categorical (grouping) variable in
\code{x}. Interactions between \code{group_var} and each variable in \code{cont_vars} are
the primary estimands. Internally, values are remapped to 0, 1, 2, … in
ascending order; the group with the lowest original value is the reference
category. Must have at least two distinct non-missing values.}

\item{cont_vars}{A non-empty character vector naming the continuous variables in \code{x} for
which interactions with \code{group_var} are to be investigated. All named
variables must exist as columns of \code{x}.}

\item{flex}{A character string controlling how FP powers are estimated and constrained
across groups. One of \code{"flex1"} (default), \code{"flex2"}, \code{"flex3"}, or
\code{"flex4"}. See the \emph{Flexibility levels} section for details.}

\item{p_interact}{Numeric. The nominal significance level used as the default threshold when
testing for interaction under \code{criterion = "pvalue"}. Default is \code{0.05}.
Ignored when \code{criterion} is \code{"aic"} or \code{"bic"}.}

\item{min_improvement}{Numeric. The minimum improvement in the selection criterion required to
include an interaction term. When \code{criterion = "pvalue"} this is a p-value
threshold (e.g. \code{0.05}); when \code{criterion = "aic"} or \code{"bic"} it is the
minimum decrease in the information criterion (must be positive; typical
values are \code{1} or \code{2}). Defaults to \code{p_interact} for p-value selection and
\code{2} for AIC/BIC selection.}

\item{include_group_var}{Logical. Whether \code{group_var} should be passed to \code{\link[=mfp2]{mfp2()}} alongside
the other adjustment variables when selecting the adjustment model in stage
one. Default is \code{FALSE}. When \code{TRUE}, the nominal significance level for
\code{group_var} is set to 1, forcing it into the adjustment model.}

\item{show_models}{Logical. Whether to print regression coefficients and standard errors for
the final interaction models. Default is \code{FALSE}, which prints only the
selected adjustment variables and model-evaluation metrics.}

\item{weights}{An optional numeric vector of non-negative observation weights of length
\eqn{n}. Default is \code{NULL} (all weights equal to 1).}

\item{offset}{An optional numeric vector of length \eqn{n} to be added to the linear
predictor. Useful for Poisson models (e.g. log of exposure time). Default
is \code{NULL} (zero offset for all observations).}

\item{cycles}{A positive integer. Maximum number of iteration cycles for the MFP
algorithm. Default is \code{10}.}

\item{scale}{A numeric vector of length \eqn{p} or a single numeric giving scaling
factors for the columns of \code{x}. Default is \code{NULL}, which lets the program
estimate scaling factors automatically (see \code{\link[=mfp2]{mfp2()}}). Set
\code{scale = 1} to disable scaling.}

\item{shift}{A numeric vector of length \eqn{p} or a single numeric giving shift terms
added to columns of \code{x} before scaling. Default is \code{NULL}, which estimates
shift factors automatically (see \code{\link[=mfp2]{mfp2()}}). Set \code{shift = 0} to
disable shifting. Shifting is applied to the data used in the final
interaction model.}

\item{df}{A numeric vector of length \eqn{p} or a single positive integer setting
the default degrees of freedom for each predictor. Degrees of freedom
equal twice the FP degree (e.g. \code{df = 2} for FP1, \code{df = 4} for FP2).
The program overrides user-supplied values based on the number of unique
values: variables with 2–3 unique values are set to \code{df = 1} (linear);
4–5 unique values to \code{df = min(2, default)}; and 6 or more unique values
retain the default. Default is \code{4}.}

\item{center}{Logical or a logical vector of length \eqn{p}. Whether to mean-centre
predictors before fitting the final interaction model. Binary covariates
are centred at the lower of their two values rather than the mean. Default
is \code{TRUE}.}

\item{subset}{An optional integer vector of positive row indices selecting a subset of
observations. Default is \code{NULL} (all observations used).}

\item{family}{A character string specifying the error distribution. One of
\code{"gaussian"} (default), \code{"binomial"}, \code{"poisson"}, or \code{"cox"}. See the
\emph{Regression families} section for details.}

\item{criterion}{A character string specifying the criterion used by \code{\link[=mfp2]{mfp2()}} to
select adjustment variables and FP degrees. One of \code{"pvalue"} (default),
\code{"aic"}, or \code{"bic"}. Note that this criterion governs adjustment-variable
selection only; the interaction test always reports p-values, AIC
differences, and BIC differences regardless of this setting.}

\item{select}{A numeric vector of length \eqn{p} or a single value in \eqn{[0, 1]}
giving the nominal significance level for backward elimination of each
predictor in the adjustment model. Setting a variable's level to \code{1}
forces it into the model. Default is \code{0.05}.}

\item{alpha}{A numeric vector of length \eqn{p} or a single value in \eqn{[0, 1]}
giving the significance level for choosing between FP degrees for each
predictor. Default is \code{0.05}.}

\item{force_keep}{An optional character vector of variable names to retain in the adjustment
model regardless of selection criteria. When \code{criterion = "pvalue"},
equivalent to setting \code{select = 1} for those variables; also effective
under AIC and BIC criteria.}

\item{xorder}{A character string controlling the order in which adjustment covariates
enter the MFP selection algorithm. \code{"ascending"} (default) enters
variables from most to least significant in a full multiple regression;
\code{"descending"} reverses this order; \code{"original"} uses the column order of
\code{x}. See \code{\link[=mfp2]{mfp2()}} for details.}

\item{fp_powers}{A named list of numeric vectors giving the candidate FP powers for each
variable. Default is \code{NULL}, which uses the standard set proposed by
Royston and Altman (1994): \eqn{\{-2, -1, -0.5, 0, 0.5, 1, 2, 3\}}, where
0 denotes the natural logarithm. Each element must be named after the
corresponding column of \code{x} and must contain at least two distinct values.}

\item{ties}{A character string specifying the method for handling tied event times in
Cox regression. One of \code{"breslow"} (default), \code{"efron"}, or \code{"exact"}.
Ignored for non-Cox families. See \code{\link[survival:coxph]{survival::coxph()}} for details.}

\item{strata}{A numeric vector or matrix defining stratification factors for Cox models.
A single combined factor is created from all supplied variables. Default is
\code{NULL} (no stratification). Currently only a single stratification factor
is supported.}

\item{nocenter}{A numeric vector of values passed to \code{\link[survival:coxph]{survival::coxph()}} to suppress
centring for specified predictors. Cox models only; ignored otherwise.}

\item{acd_vars}{An optional character vector naming continuous variables to be transformed
via the approximate cumulative distribution (ACD) transformation before FP
selection. The transformed variable is named \code{A(x)}. ACD transformation is
disabled for variables listed in \code{cont_vars}. See \code{\link[=mfp2]{mfp2()}} for
details.}

\item{zero_vars}{An optional character vector naming variables that may contain zeros or
negative values, for which the standard positivity check before log or
negative-power transformation is suppressed. Variables in \code{zero_vars} that
contain only positive values are silently reset to standard processing with
a warning.}

\item{use_ftest}{Logical. Whether to use an F-test rather than a chi-square test when
computing p-values for Gaussian models. Recommended when the sample size
is small. Default is \code{FALSE}. Has no effect for non-Gaussian families or
when \code{criterion} is not \code{"pvalue"}.}

\item{control}{A list of control parameters for the underlying fitting routine, as
returned by \code{\link[stats:glm.control]{stats::glm.control()}} (non-Cox families) or
\code{\link[survival:coxph.control]{survival::coxph.control()}} (Cox). Default is \code{NULL}, which uses the
default control parameters for the chosen family.}

\item{verbose}{Logical. Whether to print progress information during model fitting.
Default is \code{TRUE}.}

\item{digits}{A positive integer. Minimum number of significant digits displayed when
printing interaction-test results. Default is \code{3}.}

\item{data}{For \code{mfpi.formula()}: a data frame containing all variables named in
\code{formula}. Multi-level categorical variables other than \code{group_var} must
already be expanded to dummy columns.}
}
\value{
An object of class \code{"mfpi"}. Use \code{summary.mfpi()} for a formatted summary.
The object is a list with the following components:

\describe{
\item{\code{model_evaluation_metrics}}{A data frame of evaluation metrics for
the main-effects and interaction models for each variable in \code{cont_vars}.
Columns include: \code{pow_main} and \code{pow_int} (selected FP powers);
\code{dev_int} (deviance of the interaction model); \code{dev_diff} (deviance of
main-effects model minus deviance of interaction model); \code{pvalue}
(likelihood-ratio p-value); \code{AIC_int} and \code{BIC_int} (information
criteria for the interaction model); \code{AIC_diff} and \code{BIC_diff}
(main-effects minus interaction model for each criterion).}
\item{\code{adjust_terms}}{A data frame describing the FP terms selected for
the adjustment model; see \code{\link[=mfp2]{mfp2()}} for the column definitions.}
\item{\code{interaction_models}}{A named list of fitted model objects, one per
variable in \code{cont_vars}.}
\item{\code{fitted_functions}}{A named list of data frames, one per variable in
\code{cont_vars}. Each data frame contains the estimated FP function
\eqn{f_j} and its pointwise standard error \eqn{\text{se}(f_j)} at each
level \eqn{j} of \code{group_var}, together with 95\\% confidence intervals
(\code{fj_lower}, \code{fj_upper}). Columns \code{fj - f0} and \code{se(fj - f0)} give the
difference relative to the reference group, with corresponding confidence
bounds.}
\item{\code{group_var}}{The name of the grouping variable, as a character
string.}
\item{\code{show_models}}{The value of the \code{show_models} argument.}
\item{\code{flex}}{The chosen flexibility level, as a character string.}
\item{\code{group_levels_new}}{The remapped levels of \code{group_var} (0, 1, 2,
…).}
\item{\code{group_levels_original}}{The original levels of \code{group_var}.}
\item{\code{family}}{The regression family, as a character string.}
\item{\code{nobs}}{The number of observations used in model fitting.}
}
}
\description{
\code{mfpi()} investigates interactions between a categorical variable
(\code{group_var}) and one or more continuous covariates, using fractional
polynomial (FP) transformations to capture nonlinear effects. The categorical
variable is treated as a factor internally; in a typical randomised controlled
trial it represents treatment allocation, with the lowest value as the control
arm. Confounders and other prognostic variables are adjusted for via FP
transformations selected by \code{\link[=mfp2]{mfp2()}}.
}
\section{Methods (by class)}{
\itemize{
\item \code{mfpi(default)}: Default method accepting a numeric matrix \code{x} and response
vector \code{y}.

\item \code{mfpi(formula)}: Provides formula interface for \code{mfpi}.

}}
\section{The MFPI approach}{

Modelling interactions between continuous predictors with nonlinear effects
and a grouping variable (e.g. treatment) has traditionally relied on
categorising the continuous variable and applying standard interaction tests.
Categorisation is statistically inefficient and results can be sensitive to
the choice of cut-point, which in data-driven approaches introduces serious
bias (Royston, Altman, and Sauerbrei 2006).

Multivariable Fractional Polynomials for Interaction (MFPI), proposed by
Royston and Sauerbrei (2004), avoids categorisation by modelling the
relationship between the continuous predictor and the outcome using FP
functions within each level of \code{group_var}. The algorithm extends the
standard MFP variable-selection procedure by simultaneously determining
functional forms and testing whether those forms differ across groups. For
full details see Sauerbrei, Royston, and Zapien (2007).
}

\section{Flexibility levels (\code{flex})}{

Four levels of flexibility control how FP powers are estimated and
constrained between groups. The appropriate level is chosen by the analyst
based on subject-matter knowledge and sample-size considerations.

\strong{\code{flex1} (default — least flexible).} FP powers are estimated once from
the pooled main-effects model (ignoring interaction), then used unchanged
within each group. A likelihood-ratio test comparing the main-effects and
interaction models constitutes the interaction test. Because the models are
nested and no extra degrees of freedom are consumed by power estimation,
simulation studies confirm that the type-I error rate is close to its nominal
value (Royston and Sauerbrei 2014). This is the recommended starting point.

\strong{\code{flex2}.} FP powers are estimated jointly across groups (constrained to be
equal), then used for both the main-effects and interaction models. The
selected powers may differ from those under \code{flex1}, potentially altering the
test outcome. Degrees of freedom are the same as for \code{flex1}, but no
correction is made for the degrees of freedom consumed during power
estimation, so p-values are slightly anti-conservative and should be treated
as indicative.

\strong{\code{flex3}.} Separate FP powers are estimated for the main-effects model and
for the within-group functions, though the within-group powers are still
constrained to be equal across groups. Because the main-effects and
interaction models may use different FP families, they are non-nested and the
likelihood-ratio p-value is again indicative rather than definitive.
Simulation evidence suggests \code{flex3} with FP1 recovers within-group
functional forms more accurately than \code{flex1} when a true interaction is
present.

\strong{\code{flex4} (most flexible).} Like \code{flex3}, but the within-group powers are
allowed to differ between groups. The additional degrees of freedom reduce
power to detect interaction compared with \code{flex1}–\code{flex3}.

\strong{Practical guidance.} FP2 functions are more flexible and reduce the risk
of misspecification bias, but they are sensitive to influential extreme
values and lose power when the true function is monotone. If subject-matter
knowledge supports a non-monotone treatment-effect function, use \code{flex1} with
FP2. Otherwise, \code{flex3} with FP1 is preferred: it includes the linear
function as a special case and tends to produce simple, interpretable, and
transferable results (Royston and Sauerbrei 2014).

To guard against overfitting, consider examining results within a small
number of clinically defined subgroups, and seek independent validation
before drawing firm conclusions.
}

\section{Influential observations}{

Unlike categorisation, FP-based methods are sensitive to extreme values of
the continuous predictor. Before running \code{mfpi()}, examine the data for
influential points and consider whether a small number of extreme values
unduly determine the chosen functional form. Trimming or Winsorising a
handful of extreme observations is an acceptable pre-processing step and
should be reported transparently as part of the initial data analysis
(Royston and Sauerbrei 2014).
}

\section{Regression families (\code{family})}{

\code{mfpi()} accepts the same family strings as \code{\link[stats:glm]{stats::glm()}}. Use
\code{family = "gaussian"} for linear regression, \code{family = "binomial"} for
logistic regression, and \code{family = "poisson"} for Poisson regression with an
optional offset (e.g. log of exposure time). For Cox proportional-hazards
models, set \code{family = "cox"} and supply a \code{\link[survival:Surv]{survival::Surv()}} object as the
response \code{y}; only right-censored data are currently supported.
}

\section{Adjustment variables}{

MFPI operates in two stages. In the first stage, \code{\link[=mfp2]{mfp2()}} selects
adjustment variables and their functional forms from all columns of \code{x}
except the current continuous variable of interest. If no adjustment
variables survive selection, the interaction is tested without adjustment. In
the second stage, the selected (and possibly transformed) adjustment
variables are included as covariates when fitting the main-effects and
interaction models for each continuous variable listed in \code{cont_vars}.

To force adjustment variables into the model without selection or
transformation, set \code{select = 1} and \code{alpha = 0}, or pass variable names via
\code{force_keep} and set \code{df = 1} for those variables. See \code{\link[=mfp2]{mfp2()}} for
further details.
}

\references{
Royston, P. and Sauerbrei, W. (2004). A new approach to modelling
interactions between treatment and continuous covariates in clinical trials
by using fractional polynomials. \emph{Statistics in Medicine}, 23, 2509–2525.

Royston, P. and Sauerbrei, W. (2008). Interactions between treatment and
continuous covariates — a step towards individualising therapy (Editorial).
\emph{Journal of Clinical Oncology}, 26, 1397–1399.

Royston, P. and Sauerbrei, W. (2013). Interaction of treatment with a
continuous variable: simulation study of significance level for several
methods of analysis. \emph{Statistics in Medicine}, 32, 3788–3803.

Royston, P. and Sauerbrei, W. (2014). Interaction of treatment with a
continuous variable: simulation study of power for several methods of
analysis. \emph{Statistics in Medicine}, 33, 4695–4708.

Royston, P., Sauerbrei, W. and Ritchie, A. (2004). Is treatment with
interferon-alpha effective in all patients with metastatic renal carcinoma?
A new approach to the investigation of interactions. \emph{British Journal of
Cancer}, 90, 794–799.

Sauerbrei, W., Royston, P. and Zapien, K. (2007). Detecting an interaction
between treatment and a continuous covariate: a comparison of two
approaches. \emph{Computational Statistics and Data Analysis}, 51, 4054–4063.

Sauerbrei, W. and Royston, P. (2022). Investigating treatment-effect
modification by a continuous covariate in IPD meta-analysis: an approach
using fractional polynomials. \emph{BMC Medical Research Methodology}, 22, 1–13.

Royston, P. and Sauerbrei, W. (2008). \emph{Multivariable Model-Building: A
Pragmatic Approach to Regression Analysis Based on Fractional Polynomials
for Modelling Continuous Variables}. John Wiley & Sons.
}
\seealso{
\code{\link[=summary.mfpi]{summary.mfpi()}}, \code{\link[=mfp2]{mfp2()}}
}
