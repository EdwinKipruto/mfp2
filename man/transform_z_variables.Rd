% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mfpi_create_variables.R
\name{transform_z_variables}
\alias{transform_z_variables}
\title{Enumerate All FP Power Combinations for Group-Specific Interaction Variables}
\usage{
transform_z_variables(
  cont_var,
  group_var,
  shift = NULL,
  scale = NULL,
  center = FALSE,
  acdx = FALSE,
  fp_cand = NULL,
  fp_degree = 2L,
  na_replace = c("NA", "zero")
)
}
\arguments{
\item{cont_var}{A one-column numeric matrix of the continuous covariate.
Must have a column name; no missing or non-finite values permitted.}

\item{group_var}{A one-column numeric matrix of group membership with at
least two distinct values and the same row count as \code{cont_var}.}

\item{shift}{Optional numeric scalar. Shift applied to \code{cont_var} before
transformation. \code{NULL} triggers automatic estimation.}

\item{scale}{Optional numeric scalar. Scale divisor applied after shifting.
\code{NULL} triggers automatic estimation.}

\item{center}{Logical. Whether to mean-centre each transformed column.}

\item{acdx}{Logical. Whether \code{cont_var} undergoes the ACD transformation.
Default \code{FALSE}.}

\item{fp_cand}{Numeric vector of candidate FP powers. Default is the
standard Royston-Altman set \code{c(-2, -1, -0.5, 0, 0.5, 1, 2, 3)}.}

\item{fp_degree}{Positive integer. FP degree (1 = FP1, 2 = FP2). Default
\code{2}.}

\item{na_replace}{Character string; \code{"NA"} (default) or \code{"zero"}. Controls
replacement of non-finite values arising from structural zeros.}
}
\value{
A named list with four elements:
\describe{
\item{\code{z_transformed}}{List of numeric matrices, one per power
combination. Each matrix has \eqn{n} rows and \eqn{K \times J} columns,
where \eqn{K} is the number of groups and \eqn{J} is \code{fp_degree}.}
\item{\code{z_untransformed}}{Numeric matrix of group-specific linear (power =
1) variables before FP transformation, used for downstream power
application.}
\item{\code{powers_matrix}}{Numeric matrix where each row is one FP power
combination tried.}
\item{\code{znames}}{Character vector of column names for the group-specific
variables.}
}
}
\description{
For each combination of FP powers generated by \code{\link[=generate_powers_fp]{generate_powers_fp()}},
constructs the full group-specific interaction matrix via
\code{\link[=transform_matrix]{transform_matrix()}} and collects the results in a list. This is the
engine behind \code{flex2} and \code{flex4}, where the best power combination is
selected by comparing model deviances.
}
\section{Structural zeros and non-finite values}{

Group-specific variables contain structural zeros for observations outside
the relevant group. Applying log or negative-power FP transforms to zero
produces \code{-Inf} or \code{Inf}. These are not data errors; they arise purely from
the construction of the group indicator. The \code{na_replace} argument controls
how they are handled: \code{"NA"} (default) marks them as missing so they can be
distinguished from valid zeros such as \code{log(1) = 0}; \code{"zero"} replaces them
with 0 for fitting routines that cannot tolerate \code{NA}.
}

\examples{
cont <- matrix(1:6, ncol = 1); colnames(cont) <- "age"
grp  <- matrix(c(0, 1, 0, 1, 0, 1), ncol = 1); colnames(grp) <- "trt"
res  <- transform_z_variables(cont, grp, fp_cand = c(1, 0.5), fp_degree = 2)
str(res$z_transformed[[1]])

}
\seealso{
\code{create_z_variables()}, \code{\link[=generate_powers_fp]{generate_powers_fp()}},
\code{\link[=transform_matrix]{transform_matrix()}}
}
