% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mfpi_create_variables.R
\name{var_group}
\alias{var_group}
\title{Partition Variable Names into Group-Specific Subsets}
\usage{
var_group(var_prefix, var_names)
}
\arguments{
\item{var_prefix}{Character string. The common prefix of the variable names
to partition (e.g. \code{"age"} matches \code{"age01"}, \code{"age02"}, \code{"age11"}, ...).}

\item{var_names}{Character vector of all column names to search.}
}
\value{
A list of character vectors, one per unique group digit found
immediately after \code{var_prefix}.
}
\description{
Given a common variable prefix (e.g. \code{"age"}) and a character vector of
column names, returns a list where each element contains the column names
belonging to one group. Group identity is read from the single digit
immediately following the prefix.
}
\details{
This is a lookup helper used after \code{create_z_variables()} to recover which
columns belong to each level of \code{group_var}.
}
\examples{
var_group("age", c("age01", "age02", "age11", "age12", "bmi"))
# Returns: list(c("age01", "age02"), c("age11", "age12"))

}
