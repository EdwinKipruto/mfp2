% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mfpi_create_variables.R
\name{create_dummies}
\alias{create_dummies}
\title{Create Dummy Variables from a Single-Column Numeric Matrix}
\usage{
create_dummies(x, levels = NULL)
}
\arguments{
\item{x}{A numeric matrix with exactly one column and a column name. Each
distinct numeric value is treated as a category.}

\item{levels}{Optional numeric vector specifying all expected levels of \code{x}.
Useful when some levels may be absent from \code{x} (e.g. in a subset) but
dummy columns are still required for model-matrix consistency. When
supplied, the values in \code{x} must be a subset of \code{levels}; any extra values
in \code{x} not listed in \code{levels} raise an error.}
}
\value{
A numeric matrix with \code{nrow(x)} rows and \eqn{K - 1} columns, where
\eqn{K} is the number of unique levels (from \code{x} or from \code{levels}). Column
names follow the pattern \verb{<varname><level>}, e.g. \code{"trt2"}, \code{"trt3"} when
the input column is named \code{"trt"} and the non-reference levels are 2 and 3.
}
\description{
Encodes a categorical variable held in a one-column numeric matrix as a set
of binary dummy columns. The lowest numeric level is the reference category
and is omitted from the output, consistent with the default treatment
contrasts used in R's \code{\link[stats:model.matrix]{stats::model.matrix()}}.
}
\examples{
x <- matrix(c(3, 1, 2, 1, 3, 2), ncol = 1)
colnames(x) <- "group"
create_dummies(x)

# Enforce a fixed set of levels (e.g. for prediction on a subset)
x2 <- matrix(c(1, 1, 1), ncol = 1)
colnames(x2) <- "trt"
create_dummies(x2, levels = 1:3)   # trt2 and trt3 columns are all-zero

}
