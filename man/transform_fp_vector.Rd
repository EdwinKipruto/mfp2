% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transformations_vector.R
\name{transform_fp_vector}
\alias{transform_fp_vector}
\alias{transform_acd_vector}
\title{Functions to transform a variable using fractional polynomial powers or acd}
\usage{
transform_fp_vector(x, power = 1, scale = NULL, center = FALSE, shift = NULL)

transform_acd_vector(
  x,
  power = c(1, 1),
  shift = NULL,
  s = NULL,
  scale = NULL,
  center = FALSE
)
}
\arguments{
\item{x}{a vector of a predictor variable.}

\item{power}{The user-supplied FP power. Default is 1 (linear). Must be a
vector of length 2 for acd transformation.}

\item{scale}{scaling factors for x of interest. Must be positive integers.
Default is \code{NULL} and scaling factors are automatically estimated by the
program. If no scaling is needed set \code{scale = 1}.}

\item{center}{Specification of centering for variable using
the mean i.e. \code{f(x) - mean(f(x))} for continuous variables and
\code{x - min(x)} for binary variables. Default is no centering.}

\item{shift}{adjustment factors required for shifting x to positive
values. Default is \code{NULL} and adjustment factors are estimated automatically
using Royston and Sauerbrei formula iff any \code{x} <= 0.
If no shifts are needed set \code{shift = 0}.}

\item{s}{passed to \code{\link[=acd]{acd()}}.}
}
\value{
Returns a matrix of transformed variable(s). The number of columns
depends on the number of powers provided, the number of rows is equal to the
length of \code{x}.
}
\description{
These functions generate fractional polynomials for a variable similar to
\code{fracgen} in Stata. \code{transform_acd_vector} generates the acd transformation
for a variable.
}
\details{
For example, this function  generates new variables for x given the power(s)
e.g., xnew = log(x) when power = 0 and scale = F and center = F.
}
\section{Functions}{
\itemize{
\item \code{transform_acd_vector}: Function to generate acd transformation.
}}

