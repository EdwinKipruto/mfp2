% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mfpi_create_variables.R
\name{adjust_reference_category}
\alias{adjust_reference_category}
\title{Reverse or Relevel the Reference Category of a Factor}
\usage{
adjust_reference_category(x, use_top_as_ref = FALSE)
}
\arguments{
\item{x}{A factor vector, or any other vector (returned as-is if not a
factor).}

\item{use_top_as_ref}{Logical. If \code{TRUE}, the highest level (after
reversal) is used as the reference category. If \code{FALSE} (default), the
lowest level is the reference.}
}
\value{
A factor with the same values as \code{x} but with the level order
reversed and reference category updated, or the original \code{x} unchanged if
it is not a factor.
}
\description{
Reverses the level order of a factor variable and, optionally, sets the
reference to the top (highest) or bottom (lowest) level. This is a
convenience wrapper around \code{relevel()} for situations where the
default alphabetical or numerical level ordering is not appropriate.
}
\details{
When \code{x} is not a factor, it is returned unchanged.
}
\examples{
x <- factor(c("Low", "Medium", "High", "Low", "High"))
adjust_reference_category(x)                       # reference: "High" (first after reversal)
adjust_reference_category(x, use_top_as_ref = TRUE)  # reference: "Low" (last after reversal)

}
